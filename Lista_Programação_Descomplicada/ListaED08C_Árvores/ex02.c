/*Escreva uma func¸ao que conta o numero de nos de uma arvore binaria*/
#include <stdio.h>
#include <stdio.h>

struct No {
    int valor;
    struct No *esq, *dir;
};typedef struct No no;
int main(){

}

no *CriarArvore( no *raiz, int num ){
    if ( raiz == NULL ){
        
    }
}