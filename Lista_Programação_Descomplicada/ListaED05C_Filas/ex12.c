/*Escreva uma funcao que, dado duas filas, concatene as duas filas. Retorne a fila concatenada em F1. F2 deve ficar vazia.*/

#include <stdio.h>
#include <stdlib.h>

struct sNode {
    int num;
    struct sNode *next;
}; typedef struct sNode node; 
int main(){

}


